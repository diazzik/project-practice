```mermaid
activityDiagram
    title Управление кондиционером
    partition "Пользователь" {
        :Выбирает кондиционер из списка;
        :Настраивает температуру/режим;
        :Получает уведомление;
    }

    partition "Приложение" {
        :Проверяет доступ пользователя;
        :Отправляет команду кондиционеру;
        :Генерирует уведомление;
    }

    partition "Кондиционер" {
        :Получает команду;
        :Изменяет режим/температуру;
        :Отправляет данные датчика;
    }

    -->|Пользователь входит в систему| "Проверяет доступ пользователя"
    "Проверяет доступ пользователя" -->|Успешно| "Отправляет команду кондиционеру"
    "Отправляет команду кондиционеру" --> "Получает команду"
    "Получает команду" --> "Изменяет режим/температуру"
    "Изменяет режим/температуру" --> "Отправляет данные датчика"
    "Отправляет данные датчика" --> "Генерирует уведомление"
    "Генерирует уведомление" --> "Получает уведомление"
```
